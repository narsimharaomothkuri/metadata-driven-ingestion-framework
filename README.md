# metadata-driven-ingestion-framework
A reusable and extensible data ingestion framework that dynamically ingests data from any source using metadata configurations — without writing new code. The framework supports file ingestion, transformations, and loading into a target data store.
